// ---------------------------------------------------------
// 文件名: include/encoder.h
// 作用: 编码器模块的“头文件（菜单）”，只做声明，不写真正的逻辑
// ---------------------------------------------------------

#pragma once // 极客魔法：防止这个头文件被系统重复加载报错

#include <Arduino.h> // 只要用到了 Arduino 的底层功能，就必须加这个

// 【声明全局变量】
// extern 关键字的意思是：“告诉所有人，这个变量在别的房间里定义了，大家共享它”
extern volatile long pulseCount; 

// 【声明初始化函数】
// 告诉大堂经理 (main.cpp)，它只要调用这个函数，编码器就会准备好
void initEncoder();